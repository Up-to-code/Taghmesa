# تثبيت باك اند تغميسة على استضافتك

هذا الملف يشرح خطوة بخطوة كيف تفعّل قاعدة البيانات الحقيقية ولوحة التحكم على استضافة cPanel عندك.

## 1. إنشاء قاعدة البيانات

1. من لوحة تحكم الاستضافة (cPanel) افتح **MySQL Database Wizard**.
2. أنشئ قاعدة بيانات جديدة (مثلاً `taghmesa`) — لاحظ أن cPanel يضيف بادئة تلقائية مثل `username_taghmesa`.
3. أنشئ مستخدم جديد بكلمة مرور قوية، واربطه بالقاعدة مع **صلاحيات كاملة (All Privileges)**.
4. احفظ عندك: اسم القاعدة الكامل، اسم المستخدم الكامل، وكلمة المرور — ستحتاجها في الخطوة 3.

## 2. رفع الملفات

1. افتح **File Manager** في cPanel (أو استخدم FTP).
2. روح لمجلد الدومين (عادة `public_html` أو `public_html/اسم_الدومين_الفرعي`).
3. ارفع **كل محتويات** هذا المجلد (وليس المجلد نفسه) — يعني `index.html` و `config.php` و `api/` و `admin/` و `uploads/` و `.htaccess` كلها تكون مباشرة داخل `public_html`.

## 3. استيراد قاعدة البيانات

1. افتح **phpMyAdmin** من cPanel.
2. اختر القاعدة اللي أنشأتها في الخطوة 1.
3. اضغط تبويب **Import** واختر ملف `schema.sql` وارفعه، ثم Go.
4. تأكد إنه ظهرت 5 جداول: `products`, `product_sizes`, `orders`, `order_items`, `admin_users`.

## 4. ربط الموقع بقاعدة البيانات

افتح ملف `config.php` (عن طريق File Manager أو أي محرر نصوص) وعدّل السطور الأربعة بالأعلى فقط:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'اسم_القاعدة_الكامل');
define('DB_USER', 'اسم_المستخدم_الكامل');
define('DB_PASS', 'كلمة_المرور');
```

احفظ الملف.

## 5. تسجيل الدخول إلى لوحة التحكم

1. افتح `https://دومينك/admin/login.php`
2. سجّل دخول بالحساب المبدئي:
   - **اسم المستخدم:** `admin`
   - **كلمة المرور:** `Taghmesa2026!`
3. **غيّر كلمة المرور فوراً** من رابط "تغيير كلمة المرور" أعلى اللوحة.

## 6. اختبار المتجر

افتح `https://دومينك/` — يجب أن تظهر المنتجات (تُجلب الآن من قاعدة البيانات مباشرة، وليست ثابتة في الكود).

جرّب تعديل سعر منتج من لوحة التحكم، ورجّع لصفحة المتجر (تحديث الصفحة) — يجب أن يتغيّر السعر فوراً.

جرّب إتمام طلب تجريبي من المتجر، وتأكد أنه ظهر في تبويب "الطلبات" بلوحة التحكم.

## ما الذي يعمل الآن فعلياً

- المنتجات والأسعار والوصف والسعرات: **من قاعدة بيانات حقيقية**، تقدر تعدّلها من لوحة التحكم بدون لمس الكود.
- الطلبات: تُحفظ فعلياً في قاعدة البيانات وتظهر في لوحة التحكم فور تأكيد الزبون للطلب.
- لوحة تحكم محمية بكلمة مرور لإدارة المنتجات (إضافة/تعديل/رفع صور) وتحديث حالة الطلبات.

## ما الذي لا يعمل بعد (يحتاج خطوة منك)

**الدفع الإلكتروني الحقيقي (مدى / Apple Pay):** يتطلب حساب تاجر موثّق مع مزوّد دفع سعودي (مثل Moyasar أو Tap أو HyperPay). خيار "عند الاستلام" يعمل فعلياً الآن (يُسجَّل الطلب في قاعدة البيانات). خيارا "بطاقة" و"Apple Pay" يظهران في الواجهة لكن الدفع نفسه غير مفعّل تقنياً — إذا فتحت حساباً مع أحد هذول المزوّدين وأرسلت لي مفاتيح API، أقدر أربطهم بنفس نظام الطلبات الحالي مباشرة.

## نصائح أمان مهمة

- فعّل **HTTPS** (شهادة SSL) على الدومين من cPanel إذا لم تكن مفعّلة (غالباً مجانية عبر Let's Encrypt).
- لا تشارك كلمة مرور لوحة التحكم مع أي شخص، وغيّرها بشكل دوري.
- اعمل نسخة احتياطية دورية لقاعدة البيانات من phpMyAdmin (Export).
