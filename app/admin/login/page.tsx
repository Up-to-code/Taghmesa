import { LoginForm } from "@/domains/admin/components/login-form";

export const metadata = { title: "تسجيل الدخول — تغميسة" };
export default function LoginPage() { return <main className="admin-login"><LoginForm/></main>; }
